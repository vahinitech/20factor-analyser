# CI for the 20-Factor Analyser — runs on every push + PR.
#  - python-tests   : server logic without the heavy paddle/torch install.
#  - e2e            : headless Chrome — analyser app loads, print/handwriting
#                     classification, bundle is in sync with src/.
#  - e2e-recognition: full stack WITH the OCR backend; proves server recognition
#                     works on a sample. Kept fast via a cached Docker layer
#                     (paddle never re-installs) + English-only model warm-up.
name: CI

on:
  push:
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  python-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install light server deps (no paddle/torch)
        run: |
          python -m pip install --upgrade pip
          pip install -r analyser/server/requirements-core.txt
      - name: Syntax-check server modules
        run: python -m py_compile analyser/server/ppocr-server.py analyser/server/ocr_backends.py analyser/server/classify.py analyser/server/layout_filter.py analyser/server/analyser-ocr-server.py analyser/server/warmup_models.py analyser/server/benchmark_ocr.py analyser/server/geometry.py analyser/server/model_map.py analyser/server/gpu_detect.py analyser/server/computer_vision.py analyser/server/scoring.py analyser/server/detector.py analyser/server/recognizer.py analyser/server/config.py analyser/server/cache.py
      - name: Check formatting (black, line-length 79)
        run: black --check --line-length 79 analyser/build_bundle.py analyser/server/*.py analyser/server/tests/*.py
      - name: Lint (pylint, min score 9.0)
        run: |
          pylint --fail-under=9.0 analyser/build_bundle.py \
            analyser/server/analyser-ocr-server.py analyser/server/classify.py \
            analyser/server/ocr_backends.py analyser/server/ppocr-server.py \
            analyser/server/layout_filter.py analyser/server/benchmark_ocr.py \
            analyser/server/warmup_models.py analyser/server/geometry.py \
            analyser/server/model_map.py analyser/server/gpu_detect.py \
            analyser/server/computer_vision.py analyser/server/scoring.py \
            analyser/server/detector.py analyser/server/recognizer.py \
            analyser/server/config.py analyser/server/cache.py \
            analyser/server/tests/test_backends_classify.py \
            analyser/server/tests/test_server_pipeline.py \
            analyser/server/tests/test_regression_functional.py \
            analyser/server/tests/test_handwriting_only.py \
            analyser/server/tests/test_layout_filter.py
      - name: Run server unit + integration tests
        run: |
          python -m unittest -v \
            analyser.server.tests.test_backends_classify \
            analyser.server.tests.test_server_pipeline \
            analyser.server.tests.test_regression_functional \
            analyser.server.tests.test_handwriting_only \
            analyser.server.tests.test_layout_filter

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: playwright-${{ runner.os }}-
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Verify the packed bundle is in sync with src/
        run: |
          python3 analyser/build_bundle.py
          if ! git diff --quiet -- analyser/scripts/core/engine.bundle.js; then
            echo "::error::engine.bundle.js is stale. Run 'python analyser/build_bundle.py' and commit it."
            exit 1
          fi
      - name: Headless print/handwriting checks
        run: npm run test:regression:headless

  # Full stack WITH OCR. The slow part (paddle pip install) is cached as a Docker
  # layer via the GHA cache backend, so warm runs skip it entirely.
  e2e-recognition:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build analyser image (cached paddle layer)
        uses: docker/build-push-action@v6
        with:
          context: ./analyser
          file: ./analyser/server/Dockerfile.analyser
          tags: vahini/20factor-analyser:local
          load: true
          cache-from: type=gha,scope=analyser
          cache-to: type=gha,mode=max,scope=analyser
      - name: Start stack (reuses prebuilt image; English only)
        env:
          VAHINI_OCR_LANGS: en
        run: docker compose up -d --wait
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: playwright-${{ runner.os }}-
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Recognition regression (live backend)
        env:
          VAHINI_BASE_URL: http://localhost:8080
        run: npm run test:recognition
      - name: Dump server logs on failure
        if: failure()
        run: docker compose logs --tail 200 analyser
      - name: Tear down
        if: always()
        run: docker compose down -v
